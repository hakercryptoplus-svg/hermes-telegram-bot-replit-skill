model:
  provider: portkey
  name: gemini-3.5-flash

telegram:
  free_response_chats: true
  allow_from:
    - 7281928709
  admin_from:
    - 7281928709

agent:
  api_max_retries: 1
  gateway_notify_interval: 60

gateway:
  session_reset:
    mode: idle
    idle_minutes: 10080

stt:
  enabled: true

display:
  tool_progress: all
  platforms:
    telegram:
      tool_progress: all

terminal:
  cwd: /home/runner/workspace
  timeout: 30
